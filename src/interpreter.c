#include "esp32-scheme-vm"
#include "vm-arch.h"
#include "mm.h"

#define INTERPRETER

void interpreter(uint8_t * program)
{
  reg1 =
  reg2 =
  reg3 =
  reg4 =
  cont =
  env  = NIL;

  pc = program + ((*program * sizeof(cell)));

  for (;;) {
    
  }
}
